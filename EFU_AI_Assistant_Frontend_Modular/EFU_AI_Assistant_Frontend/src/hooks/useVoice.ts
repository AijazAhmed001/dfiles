import{useEffect,useState}from'react';import{voiceService}from '@/services/voiceService';export function useVoice(){const[recording,setRecording]=useState(false);const[seconds,setSeconds]=useState(0);const[processing,setProcessing]=useState(false);useEffect(()=>{if(!recording)return;const id=setInterval(()=>setSeconds(s=>s+1),1000);return()=>clearInterval(id)},[recording]);const start=()=>{setSeconds(0);setRecording(true)};const cancel=()=>{setRecording(false);setSeconds(0)};const stop=async()=>{setRecording(false);setProcessing(true);const transcript=await voiceService.transcribe();setProcessing(false);return transcript};return{recording,seconds,processing,start,stop,cancel}}
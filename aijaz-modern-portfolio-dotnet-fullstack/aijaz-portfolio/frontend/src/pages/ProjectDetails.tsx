import { Link, useParams } from 'react-router-dom';
import { projects } from '../data/content';
export default function ProjectDetails(){const {id}=useParams();const p=projects.find(x=>x.id===id);if(!p)return <main className="detail"><h1>Project not found</h1><Link to="/">Back home</Link></main>;return <main className="detail"><Link to="/">← Back</Link><span className="pill">{p.category}</span><h1>{p.title}</h1><p className="lead">{p.summary}</p><div className="detail-grid"><section><h3>Core features</h3>{p.features.map(x=><div className="feature" key={x}>✓ {x}</div>)}</section><section><h3>Technology stack</h3><div className="tags">{p.stack.map(x=><span key={x}>{x}</span>)}</div></section></div><section className="case"><h2>Case study</h2><p>This project was designed as a real-world product with clear architecture, reusable components, secure data access and deployment readiness. Replace this paragraph with screenshots, architecture diagrams, measurable results and your exact responsibilities.</p></section></main>}

import{createContext,useContext,type PropsWithChildren}from'react';import{useChat}from '@/hooks/useChat';type ChatValue=ReturnType<typeof useChat>;const C=createContext<ChatValue|null>(null);export function ChatProvider({children}:PropsWithChildren){const value=useChat();return <C.Provider value={value}>{children}</C.Provider>}export function useChatContext(){const value=useContext(C);if(!value)throw new Error('useChatContext must be used inside ChatProvider');return value}
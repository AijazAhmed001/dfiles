import{motion}from'framer-motion';import{ArrowUpRight,BarChart3,Database,Search,ShieldCheck,Sparkles}from'lucide-react';import{useNavigate}from'react-router-dom';import{SUGGESTED_PROMPTS}from '@/constants/messages';const icons=[BarChart3,ShieldCheck,Search,Database];export function WelcomeScreen(){const nav=useNavigate();return <div className="welcome-shell"><motion.div initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} className="welcome-content"><div className="ai-orb"><Sparkles size={28}/><span/></div><p className="eyebrow mt-5">EFU Intelligence</p><h1 className="welcome-title">How can I help you today?</h1><p className="welcome-copy">Ask naturally in English, Urdu, or Roman Urdu. Answers use only authorized EFU data.</p><div className="suggestion-grid">{SUGGESTED_PROMPTS.map((item,index)=>{const Icon=icons[index];return <button key={item.title} className="suggestion-card" onClick={()=>nav(`/chat/new?prompt=${encodeURIComponent(item.prompt)}`)}><span className="suggestion-icon"><Icon size={18}/></span><span className="min-w-0 flex-1 text-left"><strong>{item.title}</strong><small>{item.description}</small></span><ArrowUpRight className="suggestion-arrow" size={17}/></button>})}</div><div className="trust-strip"><span><ShieldCheck size={14}/>Role-based access</span><span><Database size={14}/>Database views only</span><span><Sparkles size={14}/>Automatic language detection</span></div></motion.div></div>}